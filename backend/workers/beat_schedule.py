# Celery Beat schedule is defined in config/settings/base.py under CELERY_BEAT_SCHEDULE
# This file is a reference for all scheduled tasks.
#
# cleanup-old-logs  — every 5 minutes  — removes task logs older than 30 days
# aggregate-stats   — every hour       — aggregates analytics data
